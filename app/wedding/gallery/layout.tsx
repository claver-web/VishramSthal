export const metadata = {
  title: "Wedding Gallery - Real Weddings at Shani Marriage Palace",
  description: "Explore photos and videos of past events, mandap setups, and real weddings at Shani Marriage Palace."
};

export default function GalleryLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
